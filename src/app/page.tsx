"use client";

import { AppSidebar } from "@/components/app-sidebar";
import { ArrowRight, BookOpen, CheckCircle2, Clock3, FilePenLine, Plus, Sparkles } from "lucide-react";
import Link from "next/link";
import { useEffect, useState } from "react";

type RecentArticle = { id: string; title: string; status: string; updated_at: string; categories: { name: string } | { name: string }[] | null; sites: { name: string } | { name: string }[] | null };
type DashboardData = { stats: { articles: number; inReview: number; published: number; activeCategories: number }; recent: RecentArticle[] };
const statusStyle: Record<string, string> = { draft: "bg-slate-100 text-slate-700", in_review: "bg-amber-100 text-amber-800", revision_requested: "bg-orange-100 text-orange-800", approved: "bg-emerald-100 text-emerald-800", published: "bg-violet-100 text-violet-800", archived: "bg-slate-100 text-slate-500" };

export default function Home() {
  const [dashboard, setDashboard] = useState<DashboardData | null>(null); const [message, setMessage] = useState("");
  useEffect(() => { void fetch("/api/cms/dashboard").then(async (response) => { const body = await response.json(); if (!response.ok) throw new Error(body.error); setDashboard(body.data); }).catch((error) => setMessage(error instanceof Error ? error.message : "Dashboard gagal dimuat.")); }, []);
  const stats = dashboard?.stats;
  const cards = [[FilePenLine, stats?.articles ?? 0, "Total artikel", "Semua draft dan artikel aktif", "bg-violet-500"], [Clock3, stats?.inReview ?? 0, "Perlu review", "Butuh tindakan editor", "bg-amber-500"], [CheckCircle2, stats?.published ?? 0, "Sudah terbit", "Tayang untuk pembaca", "bg-emerald-500"], [BookOpen, stats?.activeCategories ?? 0, "Kategori aktif", "Struktur konten tersedia", "bg-cyan-500"]] as const;
  return <main className="min-h-screen bg-[#f5f7fb] p-3 text-slate-900 sm:p-5 lg:p-7"><div className="mx-auto grid max-w-[1560px] gap-5 lg:grid-cols-[240px_minmax(0,1fr)]"><AppSidebar/><section className="min-w-0"><header className="rounded-3xl bg-slate-950 px-5 py-6 text-white shadow-xl shadow-slate-900/10 sm:px-8 sm:py-8"><div className="flex flex-col gap-6 md:flex-row md:items-end md:justify-between"><div><div className="inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1 text-xs font-semibold text-violet-200"><Sparkles size={14}/> Editorial workspace</div><h1 className="mt-4 text-3xl font-bold tracking-tight sm:text-4xl">Semua konten, satu ruang kerja.</h1><p className="mt-2 max-w-xl text-sm leading-6 text-slate-300 sm:text-base">Pantau alur editorial, artikel terbaru, dan pekerjaan yang butuh perhatian.</p></div><Link href="/articles/new" className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-white px-4 py-3 text-sm font-bold text-slate-950 transition hover:bg-violet-100 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white"><Plus size={18}/> Tulis artikel</Link></div></header><div className="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([Icon, value, label, note, color]) => <article key={label} className="group rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition duration-200 hover:-translate-y-0.5 hover:shadow-md"><div className="flex items-start justify-between"><span className={`grid h-11 w-11 place-items-center rounded-2xl text-white ${color}`}><Icon size={20}/></span><span className="text-3xl font-bold tracking-tight">{value}</span></div><p className="mt-5 font-semibold">{label}</p><p className="mt-1 text-sm text-slate-500">{note}</p></article>)}</div><div className="mt-5 grid gap-5 xl:grid-cols-[minmax(0,1fr)_320px]"><article className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between gap-4 border-b border-slate-100 px-5 py-5 sm:px-6"><div><h2 className="font-bold">Artikel terbaru</h2><p className="mt-1 text-sm text-slate-500">Perubahan terakhir yang dapat kamu akses.</p></div><Link href="/articles" className="inline-flex min-h-10 items-center gap-1 text-sm font-semibold text-violet-700 hover:text-violet-900">Semua <ArrowRight size={16}/></Link></div><div className="divide-y divide-slate-100">{dashboard?.recent.map((article) => { const category = Array.isArray(article.categories) ? article.categories[0] : article.categories; const site = Array.isArray(article.sites) ? article.sites[0] : article.sites; return <Link key={article.id} href={`/articles/${article.id}`} className="flex flex-col gap-3 px-5 py-4 transition hover:bg-slate-50 sm:flex-row sm:items-center sm:px-6"><div className="min-w-0 flex-1"><p className="truncate font-semibold">{article.title}</p><p className="mt-1 truncate text-sm text-slate-500">{site?.name ?? "-"} · {category?.name ?? "Tanpa kategori"}</p></div><div className="flex items-center justify-between gap-3 sm:justify-end"><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${statusStyle[article.status] ?? statusStyle.draft}`}>{article.status.replace("_", " ")}</span><time className="text-xs text-slate-400">{new Date(article.updated_at).toLocaleDateString("id-ID", { day: "numeric", month: "short" })}</time></div></Link>; })}{dashboard && !dashboard.recent.length && <p className="px-6 py-12 text-center text-sm text-slate-500">Belum ada artikel. Mulai dari artikel pertama.</p>}</div></article><aside className="rounded-2xl border border-violet-100 bg-gradient-to-b from-violet-50 to-white p-5 shadow-sm"><p className="text-sm font-bold text-violet-900">Alur cepat</p><p className="mt-1 text-sm leading-6 text-slate-600">Buat konten, pilih tag dan media, lalu kirim ke editor untuk ditinjau.</p><ol className="mt-6 space-y-4">{[["01", "Tulis draft", "Gunakan editor untuk teks, heading, list, dan gambar."], ["02", "Kirim review", "Editor menerima konten pada antrean review."], ["03", "Terbitkan", "Artikel siap untuk API website tenant."]].map(([number, title, description]) => <li key={number} className="flex gap-3"><span className="grid h-8 w-8 shrink-0 place-items-center rounded-full bg-white text-xs font-extrabold text-violet-700 shadow-sm">{number}</span><div><p className="text-sm font-bold">{title}</p><p className="mt-1 text-xs leading-5 text-slate-600">{description}</p></div></li>)}</ol><Link href="/articles/new" className="mt-7 inline-flex min-h-11 w-full items-center justify-center rounded-xl bg-violet-600 px-4 text-sm font-bold text-white transition hover:bg-violet-700">Mulai menulis</Link></aside></div>{message && <p role="alert" className="mt-5 rounded-xl bg-red-50 p-4 text-sm text-red-700">{message}</p>}</section></div></main>;
}
