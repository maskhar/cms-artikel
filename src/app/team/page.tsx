"use client";
import { AppSidebar } from "@/components/app-sidebar";
import { ShieldCheck, UserPlus, Users } from "lucide-react";
import { FormEvent, useEffect, useState } from "react";
type Site = { id: string; name: string }; type Role = { id: string; email: string; role: string; sites: { name: string; domain: string }[] | null };
export default function TeamPage() {
  const [roles, setRoles] = useState<Role[]>([]); const [sites, setSites] = useState<Site[]>([]); const [message, setMessage] = useState("");
  async function load() { const [roleResponse, siteResponse] = await Promise.all([fetch("/api/cms/team"), fetch("/api/cms/sites")]); const roleBody = await roleResponse.json().catch(() => null); const siteBody = await siteResponse.json().catch(() => null); if (roleResponse.ok) setRoles(roleBody?.data ?? []); else setMessage(roleBody?.error ?? "Tim tidak dapat dimuat."); if (siteResponse.ok) setSites(siteBody?.data ?? []); }
  useEffect(() => { const timer = setTimeout(() => { void load(); }, 0); return () => clearTimeout(timer); }, []);
  async function assign(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const role = String(form.get("role")); const response = await fetch("/api/cms/team", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email: form.get("email"), role, siteId: role === "admin" ? null : form.get("siteId") }) }); const body = await response.json().catch(() => null); setMessage(response.ok ? "Role berhasil ditambahkan." : body?.error ?? "Role gagal ditambahkan."); if (response.ok) { event.currentTarget.reset(); void load(); } }
  return <main className="min-h-screen bg-[#f5f7fb] p-3 sm:p-5 lg:p-7"><div className="mx-auto grid max-w-[1560px] gap-5 lg:grid-cols-[240px_minmax(0,1fr)]"><AppSidebar/><section className="min-w-0"><header className="rounded-3xl border border-slate-200/80 bg-white px-5 py-6 shadow-sm sm:px-7"><p className="text-sm font-semibold text-violet-600">Akses</p><h1 className="mt-1 text-2xl font-bold tracking-tight text-slate-950 sm:text-3xl">Tim dan role</h1><p className="mt-2 text-sm leading-6 text-slate-500">Admin memiliki akses global. Editor dan writer ditetapkan per website.</p></header><div className="mt-5 grid gap-5 xl:grid-cols-[minmax(0,1fr)_360px]"><section className="overflow-hidden rounded-3xl border border-slate-200/80 bg-white shadow-sm"><div className="flex items-center gap-3 border-b border-slate-100 px-5 py-4 sm:px-6"><span className="rounded-xl bg-violet-50 p-2 text-violet-600"><Users size={19}/></span><div><h2 className="font-semibold text-slate-900">Role aktif</h2><p className="mt-1 text-sm text-slate-500">{roles.length} penetapan akses.</p></div></div><div className="divide-y divide-slate-100">{roles.map((item) => <article key={item.id} className="flex flex-wrap items-center gap-3 px-5 py-5 sm:flex-nowrap sm:px-6"><span className="rounded-2xl bg-slate-100 p-3 text-slate-600"><ShieldCheck size={19}/></span><div className="min-w-0 flex-1"><p className="break-all font-semibold text-slate-900">{item.email}</p><p className="mt-1 text-sm text-slate-500">{item.sites?.[0]?.name ?? "Semua website"}{item.sites?.[0]?.domain ? ` · ${item.sites[0].domain}` : ""}</p></div><span className={`rounded-full px-3 py-1.5 text-xs font-semibold capitalize ${item.role === "admin" ? "bg-amber-50 text-amber-700" : item.role === "editor" ? "bg-violet-50 text-violet-700" : "bg-sky-50 text-sky-700"}`}>{item.role}</span></article>)}{!roles.length && <div className="py-14 text-center"><Users className="mx-auto text-slate-300" size={30}/><p className="mt-3 text-sm font-medium text-slate-700">Belum ada role</p></div>}</div></section><form onSubmit={assign} className="h-fit rounded-3xl border border-slate-200/80 bg-white p-5 shadow-sm sm:p-6"><h2 className="font-semibold text-slate-900">Tambah role</h2><p className="mt-1 text-sm leading-6 text-slate-500">Akun harus sudah terdaftar pada Supabase Auth.</p><label className="mt-5 block text-sm font-medium text-slate-700">Email akun<input required name="email" type="email" placeholder="writer@example.com" className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-violet-500 focus:ring-4 focus:ring-violet-100"/></label><label className="mt-4 block text-sm font-medium text-slate-700">Role<select name="role" className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm outline-none focus:border-violet-500 focus:ring-4 focus:ring-violet-100"><option value="writer">Writer</option><option value="editor">Editor</option><option value="admin">Admin global</option></select></label><label className="mt-4 block text-sm font-medium text-slate-700">Website <span className="font-normal text-slate-400">(editor/writer)</span><select name="siteId" className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm outline-none focus:border-violet-500 focus:ring-4 focus:ring-violet-100"><option value="">Pilih website</option>{sites.map((site) => <option key={site.id} value={site.id}>{site.name}</option>)}</select></label><div className="mt-4 rounded-xl bg-slate-50 p-3 text-xs leading-5 text-slate-500">Admin global mengabaikan pilihan website. Editor dan writer memerlukan website.</div><button className="mt-5 inline-flex min-h-11 w-full items-center justify-center gap-2 rounded-xl bg-violet-600 px-4 text-sm font-semibold text-white hover:bg-violet-700"><UserPlus size={18}/> Tambah role</button></form></div>{message && <p className="mt-5 rounded-2xl border border-violet-100 bg-violet-50 px-4 py-3 text-sm text-violet-800">{message}</p>}</section></div></main>;
}
