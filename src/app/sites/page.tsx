"use client";
import { AppSidebar } from "@/components/app-sidebar";
import { Globe2, Plus, Tag } from "lucide-react";
import { FormEvent, useEffect, useState } from "react";
interface Site { id: string; name: string; domain: string; slug: string; is_active: boolean }
export default function SitesPage() {
  const [sites, setSites] = useState<Site[]>([]); const [selected, setSelected] = useState(""); const [message, setMessage] = useState("");
  async function load() { const response = await fetch("/api/cms/sites"); const body = await response.json().catch(() => null); if (response.ok) setSites(body?.data ?? []); else setMessage(body?.error ?? "Website tidak dapat dimuat."); }
  useEffect(() => { const timer = setTimeout(() => { void load(); }, 0); return () => clearTimeout(timer); }, []);
  async function createSite(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const response = await fetch("/api/cms/sites", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: form.get("name"), domain: form.get("domain"), slug: form.get("slug") }) }); const body = await response.json().catch(() => null); setMessage(response.ok ? "Website dibuat." : body?.error ?? "Website gagal dibuat."); if (response.ok) { event.currentTarget.reset(); void load(); } }
  async function createCategory(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const response = await fetch(`/api/cms/sites/${selected}/categories`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: form.get("name"), slug: form.get("slug"), description: form.get("description") }) }); const body = await response.json().catch(() => null); setMessage(response.ok ? "Kategori dibuat." : body?.error ?? "Kategori gagal dibuat."); if (response.ok) event.currentTarget.reset(); }
  const selectedSite = sites.find((site) => site.id === selected);
  return <main className="min-h-screen bg-[#f5f7fb] p-3 sm:p-5 lg:p-7"><div className="mx-auto grid max-w-[1560px] gap-5 lg:grid-cols-[240px_minmax(0,1fr)]"><AppSidebar/><section className="min-w-0"><header className="rounded-3xl border border-slate-200/80 bg-white px-5 py-6 shadow-sm sm:px-7"><p className="text-sm font-semibold text-violet-600">Workspace</p><h1 className="mt-1 text-2xl font-bold tracking-tight text-slate-950 sm:text-3xl">Website</h1><p className="mt-2 text-sm leading-6 text-slate-500">Kelola tenant CMS, domain publik, dan kategori artikel.</p></header><div className="mt-5 grid gap-5 xl:grid-cols-[minmax(0,1fr)_360px]"><section className="overflow-hidden rounded-3xl border border-slate-200/80 bg-white shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-slate-100 px-5 py-4 sm:px-6"><div><h2 className="font-semibold text-slate-900">Tenant terdaftar</h2><p className="mt-1 text-sm text-slate-500">Pilih tenant sebelum membuat kategori.</p></div><span className="rounded-full bg-violet-50 px-3 py-1.5 text-xs font-semibold text-violet-700">{sites.length} tenant</span></div><div className="space-y-3 p-5 sm:p-6">{sites.map((site) => <button type="button" key={site.id} onClick={() => setSelected(site.id)} className={`flex min-h-20 w-full items-center gap-3 rounded-2xl border p-4 text-left transition ${selected === site.id ? "border-violet-300 bg-violet-50 ring-2 ring-violet-100" : "border-slate-200 hover:border-violet-200 hover:bg-slate-50"}`}><span className="rounded-2xl bg-slate-100 p-3 text-slate-600"><Globe2 size={20}/></span><span className="min-w-0 flex-1"><strong className="block truncate text-sm text-slate-900">{site.name}</strong><span className="mt-1 block break-all text-sm text-slate-500">{site.domain}</span></span><span className="hidden rounded-full bg-white px-2.5 py-1 font-mono text-xs text-slate-500 sm:block">{site.slug}</span></button>)}{!sites.length && <div className="py-12 text-center"><Globe2 className="mx-auto text-slate-300" size={30}/><p className="mt-3 text-sm font-medium text-slate-700">Belum ada website</p></div>}</div></section><div className="space-y-5"><form onSubmit={createSite} className="rounded-3xl border border-slate-200/80 bg-white p-5 shadow-sm sm:p-6"><h2 className="font-semibold text-slate-900">Tambah website</h2><label className="mt-5 block text-sm font-medium text-slate-700">Nama website<input required name="name" placeholder="Nama website" className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-violet-500 focus:ring-4 focus:ring-violet-100"/></label><label className="mt-4 block text-sm font-medium text-slate-700">Domain<input required name="domain" placeholder="portal.example.com" className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-violet-500 focus:ring-4 focus:ring-violet-100"/></label><label className="mt-4 block text-sm font-medium text-slate-700">Slug<input required name="slug" placeholder="portal-example" className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-violet-500 focus:ring-4 focus:ring-violet-100"/></label><button className="mt-5 inline-flex min-h-11 w-full items-center justify-center gap-2 rounded-xl bg-violet-600 px-4 text-sm font-semibold text-white hover:bg-violet-700"><Plus size={18}/> Buat website</button></form><form onSubmit={createCategory} className="rounded-3xl border border-slate-200/80 bg-white p-5 shadow-sm sm:p-6"><h2 className="font-semibold text-slate-900">Tambah kategori</h2><p className="mt-1 text-sm text-slate-500">{selectedSite ? `Untuk ${selectedSite.name}` : "Pilih tenant pada daftar."}</p><fieldset disabled={!selected} className="mt-5 space-y-4"><label className="block text-sm font-medium text-slate-700">Nama kategori<input required name="name" placeholder="Nama kategori" className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-violet-500 focus:ring-4 focus:ring-violet-100 disabled:bg-slate-100"/></label><label className="block text-sm font-medium text-slate-700">Slug<input required name="slug" placeholder="nama-kategori" className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-violet-500 focus:ring-4 focus:ring-violet-100 disabled:bg-slate-100"/></label><label className="block text-sm font-medium text-slate-700">Deskripsi<input name="description" placeholder="Opsional" className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-violet-500 focus:ring-4 focus:ring-violet-100 disabled:bg-slate-100"/></label><button className="inline-flex min-h-11 w-full items-center justify-center gap-2 rounded-xl border border-slate-300 px-4 text-sm font-semibold text-slate-700 hover:bg-slate-50 disabled:cursor-not-allowed disabled:bg-slate-100"><Tag size={18}/> Simpan kategori</button></fieldset></form></div></div>{message && <p className="mt-5 rounded-2xl border border-violet-100 bg-violet-50 px-4 py-3 text-sm text-violet-800">{message}</p>}</section></div></main>;
}
