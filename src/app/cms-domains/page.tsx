"use client";

import { AppSidebar } from "@/components/app-sidebar";
import { Globe2, Plus, Trash2 } from "lucide-react";
import { FormEvent, useEffect, useState } from "react";

type Site = { id: string; name: string };
type Hostname = { id: string; hostname: string; display_name: string; site_id: string | null; is_canonical: boolean; is_active: boolean; sites: { name: string } | { name: string }[] | null };

export default function CmsDomainsPage() {
  const [hostnames, setHostnames] = useState<Hostname[]>([]); const [sites, setSites] = useState<Site[]>([]); const [message, setMessage] = useState("");
  async function loadHostnames() { const response = await fetch("/api/cms/hostnames"); const body = await response.json().catch(() => null); if (response.ok) setHostnames(body?.data ?? []); else setMessage(body?.error ?? "Domain CMS gagal dimuat."); }
  useEffect(() => { const timer = setTimeout(() => { void fetch("/api/cms/sites").then((response) => response.json()).then((body) => setSites(body.data ?? [])); void loadHostnames(); }, 0); return () => clearTimeout(timer); }, []);
  async function createHostname(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const response = await fetch("/api/cms/hostnames", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ hostname: form.get("hostname"), displayName: form.get("displayName"), siteId: form.get("siteId") || null }) }); const body = await response.json().catch(() => null); if (!response.ok) { setMessage(body?.error ?? "Domain CMS gagal ditambahkan."); return; } event.currentTarget.reset(); setMessage("Domain CMS ditambahkan. Arahkan hostname tunnel ke container CMS yang sama."); void loadHostnames(); }
  async function removeHostname(hostname: Hostname) { if (!confirm(`Hapus ${hostname.hostname}? Tunnel harus dihapus terpisah.`)) return; const response = await fetch(`/api/cms/hostnames/${hostname.id}`, { method: "DELETE" }); const body = await response.json().catch(() => null); setMessage(response.ok ? "Domain CMS dihapus dari konfigurasi aplikasi." : body?.error ?? "Domain CMS gagal dihapus."); if (response.ok) void loadHostnames(); }
  return <main className="min-h-screen bg-[#f8fafc] p-5 md:p-8"><div className="mx-auto grid max-w-7xl gap-6 lg:grid-cols-[240px_1fr]"><AppSidebar/><section className="min-w-0"><p className="text-sm font-medium text-blue-600">Infrastruktur</p><h1 className="mt-1 text-3xl font-bold">CMS Domains</h1><p className="mt-1 text-sm text-slate-500">Satu container CMS menerima domain canonical dan alias tenant. Alias tidak membatasi role pengguna.</p><div className="mt-7 grid gap-6 xl:grid-cols-[1fr_360px]"><section className="rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 p-5"><h2 className="font-semibold">Hostname aktif</h2></div><div className="divide-y divide-slate-100">{hostnames.map((hostname) => { const site = Array.isArray(hostname.sites) ? hostname.sites[0] : hostname.sites; return <article key={hostname.id} className="flex items-center gap-3 p-5"><span className="rounded-xl bg-blue-50 p-2 text-blue-600"><Globe2 size={18}/></span><div className="min-w-0 flex-1"><p className="font-semibold">{hostname.display_name}</p><p className="mt-1 text-sm text-slate-500">{hostname.hostname}</p><p className="mt-1 text-xs text-slate-400">{hostname.is_canonical ? "Domain canonical" : `Website default: ${site?.name ?? "Tidak ada"}`}</p></div>{hostname.is_canonical ? <span className="text-xs font-semibold text-blue-600">Canonical</span> : <button type="button" onClick={() => removeHostname(hostname)} className="rounded-lg p-2 text-slate-400 hover:bg-red-50 hover:text-red-600" title="Hapus domain"><Trash2 size={17}/></button>}</article>; })}{!hostnames.length && <p className="p-10 text-center text-sm text-slate-500">Belum ada hostname.</p>}</div></section><form onSubmit={createHostname} className="h-fit rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold">Tambah alias CMS</h2><p className="mt-1 text-sm text-slate-500">Tambahkan DNS/tunnel setelah alias tersimpan.</p><label className="mt-4 block text-sm font-medium">Hostname<input required name="hostname" placeholder="cms.uteroindonesia.com" className="mt-1.5 w-full rounded-xl border border-slate-200 px-3 py-3"/></label><label className="mt-4 block text-sm font-medium">Nama tampilan<input required name="displayName" placeholder="Utero Indonesia CMS" className="mt-1.5 w-full rounded-xl border border-slate-200 px-3 py-3"/></label><label className="mt-4 block text-sm font-medium">Website default<select name="siteId" className="mt-1.5 w-full rounded-xl border border-slate-200 px-3 py-3"><option value="">Tanpa website default</option>{sites.map((site) => <option key={site.id} value={site.id}>{site.name}</option>)}</select></label><button className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-blue-600 px-4 py-3 text-sm font-semibold text-white"><Plus size={17}/> Tambah alias</button></form></div>{message && <p className="mt-5 rounded-xl bg-blue-50 p-3 text-sm text-blue-700">{message}</p>}</section></div></main>;
}
