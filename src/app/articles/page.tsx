import { AppSidebar } from "@/components/app-sidebar";
import Link from "next/link";
import { ArrowUpRight, Plus, Search } from "lucide-react";
import { createClient } from "@/lib/supabase/server";

const statusStyle: Record<string, string> = { draft: "bg-slate-100 text-slate-700", in_review: "bg-amber-100 text-amber-800", revision_requested: "bg-orange-100 text-orange-800", approved: "bg-emerald-100 text-emerald-800", published: "bg-violet-100 text-violet-800", archived: "bg-slate-100 text-slate-500" };

export default async function ArticlesPage() {
  const supabase = await createClient();
  const { data: articles } = await supabase.schema("artikel").from("articles").select("id, title, slug, status, updated_at, categories(name)").order("updated_at", { ascending: false });
  return <main className="min-h-screen bg-[#f5f7fb] p-3 sm:p-5 lg:p-7"><div className="mx-auto grid max-w-[1560px] gap-5 lg:grid-cols-[240px_minmax(0,1fr)]"><AppSidebar/><section className="min-w-0"><header className="flex flex-col gap-5 rounded-3xl bg-white p-5 shadow-sm sm:p-7 md:flex-row md:items-end md:justify-between"><div><p className="text-sm font-bold text-violet-700">Konten</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Artikel</h1><p className="mt-2 text-sm leading-6 text-slate-500">Kelola draft, review, dan publikasi semua artikel.</p></div><Link href="/articles/new" className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-violet-600 px-4 py-3 text-sm font-bold text-white transition hover:bg-violet-700"><Plus size={18}/> Artikel baru</Link></header><div className="mt-5 overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-3 border-b border-slate-100 p-4 sm:flex-row sm:p-5"><label className="flex min-h-11 flex-1 items-center gap-2 rounded-xl border border-slate-200 px-3 text-sm text-slate-500 focus-within:ring-2 focus-within:ring-violet-500"><Search size={17}/><input placeholder="Cari artikel…" className="w-full bg-transparent py-2 outline-none"/></label><select aria-label="Filter status artikel" className="min-h-11 rounded-xl border border-slate-200 bg-white px-3 text-sm outline-none focus:ring-2 focus:ring-violet-500"><option>Semua status</option><option>Draft</option><option>In review</option><option>Published</option></select></div><div className="divide-y divide-slate-100 md:hidden">{(articles ?? []).map((article) => <Link key={article.id} href={`/articles/${article.id}`} className="block p-4 transition hover:bg-slate-50"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="truncate font-bold">{article.title}</p><p className="mt-1 truncate text-xs text-slate-400">/{article.slug}</p></div><ArrowUpRight size={17} className="shrink-0 text-slate-400"/></div><div className="mt-4 flex items-center justify-between gap-3"><span className="text-sm text-slate-500">{((article.categories as unknown as { name: string }[] | null)?.[0]?.name) ?? "Tanpa kategori"}</span><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${statusStyle[article.status] ?? statusStyle.draft}`}>{article.status.replace("_", " ")}</span></div><time className="mt-3 block text-xs text-slate-400">Diperbarui {new Date(article.updated_at).toLocaleDateString("id-ID")}</time></Link>)}{!articles?.length && <p className="p-10 text-center text-sm text-slate-500">Belum ada artikel. Buat artikel pertama.</p>}</div><div className="hidden overflow-x-auto md:block"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-6 py-4">Judul</th><th>Kategori</th><th>Status</th><th className="px-6">Diperbarui</th></tr></thead><tbody>{(articles ?? []).map((article) => <tr key={article.id} className="border-t border-slate-100 transition hover:bg-slate-50"><td className="px-6 py-5"><Link href={`/articles/${article.id}`} className="font-bold hover:text-violet-700">{article.title}</Link><p className="mt-1 text-xs text-slate-400">/{article.slug}</p></td><td className="text-slate-500">{((article.categories as unknown as { name: string }[] | null)?.[0]?.name) ?? "-"}</td><td><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${statusStyle[article.status] ?? statusStyle.draft}`}>{article.status.replace("_", " ")}</span></td><td className="px-6 text-slate-500">{new Date(article.updated_at).toLocaleDateString("id-ID")}</td></tr>)}</tbody></table>{!articles?.length && <div className="p-12 text-center text-sm text-slate-500">Belum ada artikel. Buat artikel pertama.</div>}</div></div></section></div></main>;
}
