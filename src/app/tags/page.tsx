"use client";
import { AppSidebar } from "@/components/app-sidebar";
import { Hash, Plus, Tags } from "lucide-react";
import { FormEvent, useEffect, useState } from "react";
type Site = { id: string; name: string }; type Tag = { id: string; name: string; slug: string };
export default function TagsPage() {
  const [sites, setSites] = useState<Site[]>([]); const [siteId, setSiteId] = useState(""); const [tags, setTags] = useState<Tag[]>([]); const [message, setMessage] = useState("");
  useEffect(() => { void fetch("/api/cms/sites").then((response) => response.json()).then((body) => setSites(body.data ?? [])).catch(() => setMessage("Website gagal dimuat.")); }, []);
  async function loadTags(id: string) { setSiteId(id); if (!id) { setTags([]); return; } const response = await fetch(`/api/cms/tags?siteId=${id}`); const body = await response.json().catch(() => null); if (response.ok) setTags(body?.data ?? []); else setMessage(body?.error ?? "Tag gagal dimuat."); }
  async function createTag(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const response = await fetch("/api/cms/tags", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ siteId, name: form.get("name"), slug: form.get("slug") }) }); const body = await response.json().catch(() => null); setMessage(response.ok ? "Tag dibuat." : body?.error ?? "Tag gagal dibuat."); if (response.ok) { event.currentTarget.reset(); void loadTags(siteId); } }
  return <main className="min-h-screen bg-[#f5f7fb] p-3 sm:p-5 lg:p-7"><div className="mx-auto grid max-w-[1800px] gap-5 lg:grid-cols-[240px_minmax(0,1fr)]"><AppSidebar/><section className="min-w-0"><header className="rounded-3xl border border-slate-200/80 bg-white px-5 py-6 shadow-sm sm:px-7"><p className="text-sm font-semibold text-violet-600">Taksonomi</p><h1 className="mt-1 text-2xl font-bold tracking-tight text-slate-950 sm:text-3xl">Tag artikel</h1><p className="mt-2 text-sm leading-6 text-slate-500">Kelola tag per website untuk dipasang pada artikel.</p></header><div className="mt-5 grid gap-5 xl:grid-cols-[minmax(0,1fr)_360px]"><section className="rounded-3xl border border-slate-200/80 bg-white shadow-sm"><div className="border-b border-slate-100 px-5 py-4 sm:px-6"><label className="block max-w-md text-sm font-medium text-slate-700">Website<select value={siteId} onChange={(event) => void loadTags(event.target.value)} className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm outline-none focus:border-violet-500 focus:ring-4 focus:ring-violet-100"><option value="">Pilih website</option>{sites.map((site) => <option key={site.id} value={site.id}>{site.name}</option>)}</select></label></div><div className="p-5 sm:p-6">{siteId && tags.length > 0 && <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{tags.map((tag) => <article key={tag.id} className="rounded-2xl border border-slate-200 bg-slate-50/60 p-4"><div className="flex gap-3"><span className="rounded-xl bg-violet-100 p-2 text-violet-700"><Hash size={17}/></span><div className="min-w-0"><p className="font-semibold text-slate-900">{tag.name}</p><p className="mt-1 break-all text-sm text-slate-500">{tag.slug}</p></div></div></article>)}</div>}{(!siteId || !tags.length) && <div className="py-12 text-center"><Tags className="mx-auto text-slate-300" size={30}/><p className="mt-3 text-sm font-medium text-slate-700">{siteId ? "Belum ada tag" : "Pilih website dulu"}</p><p className="mt-1 text-sm text-slate-500">{siteId ? "Buat tag pertama untuk tenant ini." : "Daftar tag dipisahkan per tenant."}</p></div>}</div></section><form onSubmit={createTag} className="h-fit rounded-3xl border border-slate-200/80 bg-white p-5 shadow-sm sm:p-6"><h2 className="font-semibold text-slate-900">Tambah tag</h2><p className="mt-1 text-sm leading-6 text-slate-500">Slug gunakan huruf kecil dan tanda hubung.</p><label className="mt-5 block text-sm font-medium text-slate-700">Nama tag<input required disabled={!siteId} name="name" placeholder="Teknologi" className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-violet-500 focus:ring-4 focus:ring-violet-100 disabled:bg-slate-100"/></label><label className="mt-4 block text-sm font-medium text-slate-700">Slug<input required disabled={!siteId} name="slug" placeholder="teknologi" className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-violet-500 focus:ring-4 focus:ring-violet-100 disabled:bg-slate-100"/></label><button disabled={!siteId} className="mt-5 inline-flex min-h-11 w-full items-center justify-center gap-2 rounded-xl bg-violet-600 px-4 text-sm font-semibold text-white hover:bg-violet-700 disabled:cursor-not-allowed disabled:bg-slate-300"><Plus size={18}/> Buat tag</button></form></div>{message && <p className="mt-5 rounded-2xl border border-violet-100 bg-violet-50 px-4 py-3 text-sm text-violet-800">{message}</p>}</section></div></main>;
}
