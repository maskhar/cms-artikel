import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
export async function DELETE(_request: Request, { params }: { params: Promise<{ assetId: string }> }) { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return NextResponse.json({ error: "Unauthenticated" }, { status: 401 }); const { assetId } = await params; const { data, error } = await supabase.schema("artikel").from("media_assets").delete().eq("id", assetId).select("storage_path").maybeSingle(); if (error) return NextResponse.json({ error: error.message }, { status: 400 }); if (!data) return NextResponse.json({ error: "Media tidak ditemukan atau tidak dapat dihapus." }, { status: 404 }); return NextResponse.json({ data }); }
